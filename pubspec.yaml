name: light_house
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  custom_qr_generator: ^0.1.2
  esc_pos_bluetooth: ^0.4.1
  esc_pos_utils: ^1.1.0
  fl_chart: ^0.69.0
  flutter_bluetooth_printer: ^2.9.1
  flutter_slidable: ^3.1.1
  flutter_thermal_printer: ^0.0.4
  internet_connection_checker: ^1.0.0+1
  intl: ^0.19.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.4

dependency_overrides:
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
